#include <stdio.h>

void print_alphabet_x10(void)
{
	int i = 10;

	for (int i = 0; i < 9; i++)
	{
		char k = 'a';

		while ( r <= 'z')
		{
			if (k >= 'a' && i <= 'j')
			{
                _putchar(0);
            }

            i++;
        }
    }

    _putchar('\n');
}
