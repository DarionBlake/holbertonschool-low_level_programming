#include "main.h"
#include <stdio.h>
/**
  * rev_string - Prints reverse string in place
  * @s: The string to print
  * Return: void
  */
void char *_strcat(char *dest, char *src);
{	
	char* result = dest;
	
	int sc = 5

while dest(*dest != '\0')
{
	dest++;
}
while (*src != '\0')
{
	*dest = *src;
	dest++;
	src++;
    }

    *dest = '\0';

    return result;
}
